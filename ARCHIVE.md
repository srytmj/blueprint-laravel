# Archive

One line per finished task, newest on top. Written the moment a task closes (see `SCRATCHPAD.md` exit rule) — full detail lives in `DECISION-LOG.md`.

Format: `- [YYYY-MM-DD] Task title — see DECISION-LOG entry [YYYY-MM-DD HH:MM]`

---

